[project]
name = "lingo-parole-in-gioco"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "numpy>=2.3.3",
    "pandas>=2.3.2",
    "uvicorn>=0.37.0",
]

[tool.uv.workspace]
members = ["app"]
